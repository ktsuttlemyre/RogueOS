version: "3.9"
services:
    psitransfer:
        ports:
            - 0.0.0.0:3000:3000
        environment:
            - PSITRANSFER_ADMIN_PASS=secret
        volumes:
            - $PWD/data:/data
        image: psitrax/psitransfer
# data volume needs UID 1000
